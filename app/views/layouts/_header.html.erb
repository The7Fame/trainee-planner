<nav class="navbar navbar-expand-lg navbar-light bg-light">
  <div class="container">
    <div class="collapse navbar-collapse">
      <ul class="navbar-nav ">
        <li class="nav-item">
          <h3 class="navbar-brand"><%= t('nav.company') %></h3>
        </li>
      </ul>
    </div>
    <% if current_user %>
      <%= render partial: 'users/partials/user_dropdown' %>
    <% else %>
      <%= render partial: 'users/partials/entry' %>
    <% end %>
    <%= render partial: 'layouts/locales' %>
  </div>
</nav>
